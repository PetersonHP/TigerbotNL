from pokerkit import State


# list of child nodes (take action -> other stuff happens -> new InfoSet)
# terminal? -> rrrexpected reward?
class InfoSet:
    def __init__(self, game_state: State, my_index: int) -> None:
        
        pass